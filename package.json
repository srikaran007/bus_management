{
  "name": "bus_management",
  "version": "1.0.0",
  "private": true,
  "description": "Bus management monorepo",
  "scripts": {
    "start:frontend": "npm --prefix frontend start",
    "start:backend": "npm --prefix backend start",
    "test:backend": "npm --prefix backend test"
  }
}
